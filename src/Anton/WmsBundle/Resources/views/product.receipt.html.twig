{% extends "@AntonWms/admin.base.html.twig" %}
{% block sonata_head_title %}Приход товара{% endblock %}
{% block content %}
    <div>
        Выберите категорию склада слева. Справа станут доступны неотправленные на склад продукты, которые
    могут быть отправлены в склады по данной категории. Далее выберите конкретный склад. Внизу страницы станут
            доступны свободные места в данном складе.
    </div>
    <hr>
    <div class="uk-grid-divider uk-child-width-expand@s product-receipt" uk-grid>
        <div>
            <h2>Склад</h2>
            <div class="warehouse__category">
                <label for="warehouse-category">Категория склада</label>
                <select id="warehouse-category">
                    <option>Не выбрано</option>
                    {% for category in categories %}
                    <option value="{{ category.id }}">{{ category.name }}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="warehouse">
                <label for="warehouse">Склад</label>
                <select id="warehouse">
                    <option>Не выбрано</option>
                </select>
            </div>
            <div class="warehouse__info">
            </div>
        </div>
        <div>
            <h2>Товар</h2>
            <div class="product">
                <label for="product">Неучтённые товары</label>
                <select id="product">
                    <option>Не выбрано</option>
                </select>
            </div>
            <div class="product__info">
            </div>
        </div>
    </div>
    <div class="product-receipt__send uk-flex-center" uk-grid>
        <div>
            <label for="product-map">Место на складе</label>
            <select id="product-map">
                <option>Не выбрано</option>
            </select>
            <button id="product-send__button" class="uk-button uk-button-primary uk-width-1-1 uk-margin-small-bottom">Отправить на склад</button>
        </div>
    </div>
{% endblock %}